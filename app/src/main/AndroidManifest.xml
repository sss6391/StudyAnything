<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.hkit.smartcloset">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SpeakerActivity"></activity>
        <activity android:name=".LightActivity" />
        <activity android:name=".AirInfoActivity" />
        <activity android:name=".AutoActivity" />
        <activity android:name=".ThemeActivity" />
        <activity
            android:name=".ui.login.aaaaaaa"
            android:label="@string/title_activity_aaaaaaa" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".SaveNewcloActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".ReturnActivity" />
        <activity android:name=".SelectionActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>